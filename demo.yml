port: 11254
indexer:
  program: ctags
  args:
    - -R
    - -e
    - --languages=ruby
  tag_file: TAGS
  type: rvm
  exclude:
    - .git
    - log
    - tmp
  max_frequency: 5s
projects:
  - path: ~/Workspace/agnostic_backend
