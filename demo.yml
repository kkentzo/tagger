indexer:
  program: ctags
  args:
    - --languages=ruby
    - -R
    - -e
  tag_file: TAGS
  exclude:
    - .git
    - log
    - tmp
  max_frequency: 5s
projects:
  - path: ~/Workspace/agnostic_backend
